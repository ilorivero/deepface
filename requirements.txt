# Dependências diretas da aplicação
# Projeto validado com Python 3.10, 3.11 e 3.12

deepface>=0.0.79
opencv-python>=4.8.0
flask>=2.3.0
numpy>=1.23,<2.0

# Backend de deep learning (seleção por plataforma)
tensorflow-macos>=2.16.0; platform_system == "Darwin" and platform_machine == "arm64" and python_version < "3.13"
tensorflow>=2.16.0; (platform_system != "Darwin" or platform_machine != "arm64") and python_version < "3.13"

# Compatibilidade exigida pelo DeepFace com TensorFlow 2.16+
tf-keras>=2.16.0; python_version < "3.13"